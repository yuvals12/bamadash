const calc= (operand1, operand2, operator)=>{
    let answer;
    if (operator=="*") {
        
        answer=operand1*operand2;
        console.log(answer);
    }
    if (operator=="/") {
        
        answer=operand1/operand2;
        console.log(answer);
    }
    if (operator=="-") {
        
        answer=operand1-operand2;
        console.log(answer);
    }
    else {
        
        answer=operand1-operand2;
        console.log(answer);
    }
           
}
const compact= (array)=>{
     array
        
     }



